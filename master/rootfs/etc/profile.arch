#!/bin/sh

export PATH=$PATH:/opt/lantiq/bin:/usr/local/bin
export LD_LIBRARY_PATH=/lib:/usr/lib:/usr/local/lib

alias la='ls -la'
alias ..='cd ../..'
alias omci='omci_pipe.sh'
alias dsl='dsl_pipe'

# if possible, adapt to size of terminal
[ -x /usr/bin/resize ] && resize
